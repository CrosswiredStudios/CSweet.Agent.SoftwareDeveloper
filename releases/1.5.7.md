# 1.5.7

- Uses C-Sweet Agent SDK 3.45.1 workspace limits supplied by the runtime, so repository snapshot size, expanded size, and file-count policy can be changed without rebuilding Daniel.
- Preserves fail-closed workspace extraction and upload checks while allowing larger configured development repositories to recover and continue automatically.
