# 1.9.2

- Makes the review URL the first line of the final handoff, with a plain-language invitation to try the build, readable expiry information, and links to source and task notes. Raw coding reports and internal identifiers no longer obscure the link.
- Sends the saved review handoff before marking the deployment task complete. Interrupted delivery retries the same message key without rebuilding.
- Uses shorter, first-person onboarding, progress, and blocker messages. Full actionable diagnostics remain on the ticket.
- Runs the source snapshot's package.json test script with npm --offline test in the cached Node container before building the application image. Missing test scripts and test failures prevent delivery and use the existing bounded repair flow. Non-Node deployments are unchanged.
- Directs integration validation to exercise the application's real entry point and feature wiring instead of relying on isolated modules or copied simulations.

No new grants or configuration are required. The existing compute command grant covers validation. Update Daniel to 1.9.2 for future attempts; previously completed tasks, deployments, and messages are not rewritten or automatically rerun.
