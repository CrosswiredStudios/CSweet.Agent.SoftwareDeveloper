# 1.6.1

- Recover once from an exhausted replacement budget when an expired timed instance is being replaced under the until-release policy.
- Persist the recovery marker and preserve monotonic allocation keys, committed source, and test results across restarts and requeues.
- Keep disabled retries disabled, wait for confirmed teardown, and never transfer an unresolved command or network grant to replacement compute.
