# 1.5.1

- Retain source changes and exact validation diagnostics when a planned coding task fails validation.
- Retry each failed planned task automatically for two bounded repair passes before blocking it for human attention.
- Restore the retained failed snapshot on the next callback so Daniel can inspect and repair his previous work without requeueing the epic, story, or task.
- Treat expected negative-path failures as passing only when their enclosing validation command exits successfully.

Requires the C-Sweet planning and workspace capabilities introduced for Software Developer 1.5.0.
