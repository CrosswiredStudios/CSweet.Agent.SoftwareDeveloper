# 1.11.2

Test the prebuilt `.csab` release install path: the CSweet app should detect the tagged release bundle and install it via download/verify/install instead of queueing a fleet source build. No behavior change from 1.11.1.
