# 1.8.0

- Requests and durably reconciles one assigned Linux development workspace before accepting development work.
- Continues onboarding, requirements discussion, planning, ticket decomposition, and repository reservation while compute is unavailable.
- Keeps development tickets Ready and unclaimed until assigned compute is Ready, then uses revision-fenced exact claims.
- Sends one manager blocker message per terminal compute incident and completes onboarding independently of compute readiness.
- Uses concise, deterministic repository names and reuses the assigned workspace across development tickets.
