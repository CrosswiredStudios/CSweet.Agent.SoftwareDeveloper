# 1.5.4

- Route planned ticket execution through the bounded continuation harness instead of failing after a single model turn.
- Give complex implementation tickets up to six bounded harness turns so the coding model can finish edits, run validation, and write its required structured outcome.
- Make the final continuation explicitly require validation and outcome finalization instead of allowing another plan-only response.
- Preserve bounded failure behavior when the model never performs work.
- Compact coding sessions below C-Sweet's broker message boundary so long tool transcripts do not fail before reaching the configured LLM provider.
- Recover an oversized coding session automatically by starting a clean model session against the source files retained in the assignment workspace.
- Cap older saved context-window settings to the broker-safe budget without requiring users to edit existing agent configuration.
