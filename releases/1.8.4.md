# 1.8.4

- Accept verified unchanged work with an explicit empty changed-file list, a summary, and fresh validation evidence.
- Report malformed completion fields precisely and accept UTF-8 BOMs written by file tools.
- Save planned-task completion evidence and publication results so retries finish platform updates without repeating the coding model.
- Upload partial source after incomplete turns and recoverable/non-cancellation model failures, while honoring cancellation and existing grants.
- Keep validation repairs bounded by the configured limit and show completion-report failures in the owner-facing blocker.
- Clarify that Python static checks and Docker health checks do not automatically run the Node test suite.

Existing saved plans remain compatible. No capabilities, grants, SDK versions, or runtime images change.
