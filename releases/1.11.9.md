# 1.11.9

- Handles installation onboarding through the new SDK `OnOnboardedAsync` hook instead of the compute event partial.
- Falls through to the SDK base dispatch for events the agent does not handle directly.
- Requires `CSweet.Agent.SDK` 3.55.0 for the onboarding dispatch contract.

No runtime behavior, grants, or configuration changed.
