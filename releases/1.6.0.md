# 1.6.0

- Request compute until explicitly released by default; the lifetime remains configurable and grant-controlled.
- Preserve full Docker build logs inside compute and supply the failure tail to the repair model, avoiding pages of passing tests hiding the error.
- Make deployment and task-validation repair budgets configurable.
- Remove the independent one-hour Hello World service timer; compute owns the lifetime.
