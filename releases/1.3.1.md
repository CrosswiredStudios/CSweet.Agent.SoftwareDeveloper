# 1.3.1

- Resume waiting compute tasks when Core reports its actual Running queue status.
- Requeue the correlated personal task through the SDK on compute events so the normal claim handler owns commands, publication, completion and failures.
- Ignore duplicate wakes once a task is ready, actively claimed or complete. Retain the bounded recovery deadline for missed events.
