# 1.5.8

- Recover deployment from failed or expired compute after teardown, preserving source commits and test results.
- Configure the maximum number of replacement instances per development task; refresh placement defaults for each replacement.
- Preserve unresolved commands and require a separate network grant for every new instance.
- Deduplicate repeated identical blocker messages across attention reviews.