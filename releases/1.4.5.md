# 1.4.5

- Authorize harness read/write tools only inside the assigned file store so unattended development does not stop at an invisible approval prompt. Broad automatic approval stays disabled.
- Continue an incomplete coding response in the same session, with a three-turn bound; report unhandled approval requests explicitly.
- Retain the source commit and request one grant-checked replacement when a test instance has expired and no command is unresolved. Network access remains a separate explicit grant.
- Add regression coverage for real harness tool execution, early responses, approval pauses, and expired-instance recovery.
