# 1.10.1

- Source-control blocker comments display the specific server error, failure code, HTTP status, diagnostic ID, and relevant recovery step instead of a generic capability failure or raw JSON envelope.
- Requires the accompanying platform workspace diagnostic changes for complete detail. Older servers retain the existing fallback report.
- No new capabilities or grants. Retained credentials and host paths remain redacted.
