# 1.6.4

- Upgrade to C-Sweet Agent SDK 3.46.1 and Work Management Contracts 3.22.0 so runtime failures carry a safe failure type, HTTP status when available, and diagnostic ID.
- Allow C-Sweet to show actionable recovery information without exposing provider responses, credentials, or stack traces.
