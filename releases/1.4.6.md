# 1.4.6

- Fix startup reconnect loops when C-Sweet supplies optional compute settings declared in the agent manifest. Blank saved compute settings now pass runtime configuration validation.
- Add a manifest-to-runtime configuration contract test covering every field and an update containing blank optional compute values.
- Compute placement remains platform-managed, with separate explicit networking grants.
