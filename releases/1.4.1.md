# 1.4.1

- Fix immediate "Invalid assignment workspace" failures after repository preparation. Download source from Core into the isolated agent runtime before invoking the coding harness.
- Upload edited source before publishing, so the Git commit contains the application's actual files.
- Recover existing requeued tasks and missing runtime workspaces using the same task and repository. Preserve existing local edits on repeated materialization.
- Report the current blocker after each retry instead of hiding new errors behind an earlier chat-message key.
- Upgrade to SDK 3.44.0 and request `git.workspace.sync.v1`. Review this added declaration during the normal agent update. No host mounts, Git credentials or network authority are added.
- Requires the matching C-Sweet workspace-transfer update. That update also delivers personal-task status notifications so Kanban cards follow requeue, running, blocked and completed transitions.

Snapshot limits: 512 KiB compressed, 16 MiB content and 4,096 files. Existing compute and explicit network-grant limits continue to apply.
